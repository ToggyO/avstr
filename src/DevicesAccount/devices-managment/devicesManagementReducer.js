// import {} from './actions';


const initialState = {
    //
};


const devicesManagementReducer = (state = { ...initialState }, { type /* , data */ }) => {
    switch (type) {
        default:
            return state;
    }
};

export default devicesManagementReducer;
