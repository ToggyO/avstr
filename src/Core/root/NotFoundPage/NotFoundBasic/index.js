import React from 'react';

import NotFoundPage from '../index';

import styles from './index.module.scss';

const NotFoundBasic = () => (
    <div className={styles.wrap}>
        <NotFoundPage />
    </div>
);

export default NotFoundBasic;
