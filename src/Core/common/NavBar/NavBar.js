import React from 'react';
import PropTypes from 'prop-types';

const NavBar = ({ btnHandler }) => {
    return (
        <nav>

        </nav>
    );
};

NavBar.propTypes = {

};

export default NavBar;
